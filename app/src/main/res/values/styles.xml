<resources>

    <!-- 🟦 Base Theme using Material Components -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- شفافية شريط الحالة -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:fitsSystemWindows">true</item>

        <!-- ✅ الخط العام -->
        <item name="fontFamily">@font/montserrat_regular</item>

        <!-- ✅ ربط نمط القائمة المنبثقة -->
        <item name="popupMenuStyle">@style/PopupMenuStyle</item>
        <item name="android:popupMenuStyle">@style/PopupMenuStyle</item>
    </style>

    <!-- 🟪 Fullscreen Theme -->
    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <!-- 🔷 Custom ActionBar style -->
    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <!-- 🎨 نمط القائمة المنبثقة (Popup Menu) -->
    <style name="PopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <!-- خلفية مخصصة بحواف ناعمة -->
        <item name="android:popupBackground">@drawable/bg_popup_menu</item>
        <!-- مظهر النصوص داخل القائمة -->
        <item name="android:textAppearance">@style/PopupMenuTextAppearance</item>
    </style>

    <!-- 🖋️ تنسيق نصوص عناصر القائمة المنبثقة -->
    <style name="PopupMenuTextAppearance" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:fontFamily">@font/montserrat_regular</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">12dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

</resources>
